{
  "name": "pomona-executor",
  "version": "0.1.0",
  "private": true,
  "description": "Pomona Finance off-chain executor — chain watcher, trigger logic, batched compound transactions",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/service/main.ts",
    "test": "vitest"
  },
  "dependencies": {
    "@lucid-evolution/lucid": "latest",
    "@minswap/sdk": "latest",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}

# ADApose web

Not started. Never scaffolded for the original auto-compounding app (now
archived in `legacy/`, which never had a `web/` beyond this stub either), and
nothing's been decided yet for the current √k-based direction — see the root
`README.md` and `docs/decisions.md` D26. React + Mesh SDK (CIP-30) was the
old app's plan; no reason yet to assume or discard that for whatever comes
next.
